type parameter = unit;
type storage = unit;

type return_ = [list<operation>, storage];

let main = ([_action, store]: [parameter, storage]): return_ => {

  if (Tezos.amount > (0 as tez)) {
    failwith("This contract does not accept tokens yet.") as return_;
  }
  else {
    return [
      (list([]) as list<operation>),
      store
    ];
  }
}
